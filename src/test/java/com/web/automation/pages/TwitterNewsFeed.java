package com.web.automation.pages;

import org.openqa.selenium.WebDriver;

public class TwitterNewsFeed extends BasePage{

    /**
     * Constructor.
     *
     * @param pDriver WebDriver
     */
    public TwitterNewsFeed(WebDriver pDriver) {
        super(pDriver);
    }
}
