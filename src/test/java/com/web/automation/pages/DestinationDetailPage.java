package com.web.automation.pages;

import org.openqa.selenium.WebDriver;

public class DestinationDetailPage extends BasePage{
    /**
     * Constructor.
     *
     * @param pDriver WebDriver
     */
    public DestinationDetailPage(WebDriver pDriver) {
        super(pDriver);
    }
}
